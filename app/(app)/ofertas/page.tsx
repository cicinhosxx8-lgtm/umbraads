import { PhasePlaceholder } from "@/components/shell/PhasePlaceholder";

export default function OfertasPage() {
  return (
    <PhasePlaceholder
      titulo="Ofertas Escaladas"
      fase="Fase 3"
      descricao="O feed com filtros do que já vende no Facebook Ads."
    />
  );
}
